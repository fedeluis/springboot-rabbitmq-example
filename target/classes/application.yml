server:
    port: 9292
spring:
    application:
        name: springboot-rabbitmq-example
    datasource:
        url: jdbc:postgresql://localhost:5432/restaurant_example
        username: postgres
        password: password
    jpa:
        hibernate.ddl-auto: update
        show-sql: true
spring.jpa.properties.hibernate.format_sql: true
spring.jpa.properties.hibernate.dialect: org.hibernate.dialect.PostgreSQLDialect